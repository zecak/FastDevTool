<Window x:Class="FastDevTool.MVVM.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FastDevTool.MVVM"
        mc:Ignorable="d"
        xmlns:s="https://github.com/canton7/Stylet"
        d:DataContext="{d:DesignInstance local:ShellViewModel}"
        Title="主界面" MinHeight="600" MinWidth="800" Style="{DynamicResource WindowStyle1}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Res/ControlStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <StackPanel>
            <TextBlock FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Name}">
            </TextBlock>
            <Button Content="Say Hello" Command="{s:Action SayHello}"></Button>
            <StackPanel>
                <TextBlock FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding MyPerson.FullName }">
                </TextBlock>
            </StackPanel>
            <Button Content="Add User" Command="{s:Action AddUser}"></Button>
            <ComboBox ItemsSource="{Binding datas}" DisplayMemberPath="FullName" SelectedValuePath="ID" SelectedValue="{Binding ID}" IsReadOnly="True" >

            </ComboBox>
        </StackPanel>
    </Grid>
</Window>
